# arsa-frontend

This is the frontend portion for our API server, which serves geographical data of natural disasters

To build production, run `npm run build`.

To start development client, run `npm run dev`.

To set up, run `npm install`.
