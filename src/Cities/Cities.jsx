import { Link } from "react-router-dom"

function Cities() {
    return (
        <div>
            Cities
            <br />
            <Link to="/">Home</Link>
        </div>
    )
}

export default Cities